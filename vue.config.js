var path = require('path');

module.exports = {
  /* Useful options for custom build:
   * outputDir: process.env.NODE_ENV === 'production' ? './build/' : 'dist', 
   * assetsDir: process.env.NODE_ENV === 'production' ? './build/assets/' : '',
   */
  publicPath: '/',
}
